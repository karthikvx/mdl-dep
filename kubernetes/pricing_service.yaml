# Pricing Service
apiVersion: v1
kind: Service
metadata:
  name: pricing-service
  namespace: mortgage-system
spec:
  selector:
    app: pricing-service
  ports:
  - protocol: TCP
    port: 5001
    targetPort: 5001
  type: ClusterIP
